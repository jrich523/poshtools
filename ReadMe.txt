In order to add a new Item or Project template you need to do the following:

1) make sure VS SDK is installed
2) create new Extension project for item/project, the project folder should match with the file system folder.
3) add the asset in the PowerShellTools\Manifest\12.0\source.extension.vsimanifest
4) make sure its set to build: Build - Configuration Manager - check Build for the project